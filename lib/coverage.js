'use strict';
function coverage({expected=0,received=0,waiver=false,sourceFreshness=1,spatialConfidence=1}){const denominator=Math.max(0,Number(expected)||0),num=Math.max(0,Number(received)||0);const ratio=denominator?Math.min(1,num/denominator):(waiver?1:0);const cap=Math.max(0,Math.min(1,ratio*sourceFreshness*spatialConfidence));return {expected:denominator,received:num,waiver,coverage_ratio:ratio,confidence_cap:cap,verdict_language:cap>=.9?'high coverage':cap>=.65?'moderate coverage':cap>=.35?'limited coverage':'insufficient coverage',rule:'Final confidence may not exceed monitoring coverage × source freshness × spatial applicability.'}}
function calibrate(cases=[]){const bins=[0,.2,.4,.6,.8,1].map((lo,i)=>({lo,hi:i===4?1:lo+.2,n:0,correct:0}));for(const c of cases){const p=Math.max(0,Math.min(.999999,Number(c.predicted_probability)));const b=bins[Math.min(4,Math.floor(p*5))];b.n++;b.correct+=c.correct?1:0}return bins.map(b=>({...b,observed_accuracy:b.n?b.correct/b.n:null,label:b.hi<=.4?'unlikely':b.lo>=.8?'very likely':b.lo>=.6?'likely':'uncertain'}))}
module.exports={coverage,calibrate};
